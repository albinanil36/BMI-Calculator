import 'package:flutter/material.dart';

// defining colors used

const primaryColor = Color(0xFF1d1936);
const secondaryColor = Color(0xFF282442);
const whiteColor = Colors.white;
final fadedWhite = Colors.grey[400];
final buttonColor = Colors.red[400];